/**
 * 경고: 이 모델 클래스는 현재 사용되지 않습니다.
 * 대신 lib/core/utils/note_tutorial.dart 파일 내부에 정의된 TutorialStep 클래스를 사용하세요.
 * 이 파일은 향후 제거될 예정입니다.
 */

/*
/// 튜토리얼의 각 단계를 나타내는 모델 클래스
class TutorialStep {
  /// 제목
  final String title;
  
  /// 설명
  final String description;
  
  /// 이미지 경로 (없을 수 있음)
  final String? imagePath;
  
  const TutorialStep({
    required this.title,
    required this.description,
    this.imagePath,
  });
}
*/ 